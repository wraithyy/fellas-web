---
import { Image } from '@astrojs/image/components'
import { Button } from '../Button'
const {
  frontmatter: { title, description, image }
} = Astro.props
---

<div
  class="max-w-sm bg-white border border-gray-200 shadow group hover:scale-105 ease-in-out duration-300">
  <>
    <div class="overflow-hidden">
      <Image
        class="object-fill group-hover:scale-125 ease-in-out duration-300 m-0"
        src={image}
        alt=""
      />
    </div>
    <div class="p-5">
      <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">
        {description}
      </p>
      <div class="flex justify-center">
        <Button text="Více info" />
      </div>
    </div>
  </>
</div>;
